const express = require("express");
const path = require("path");

const ExpressServer = () => {
  const app = express();

  // POST

  // GET

  // PATCH

  // DELETE

  return app;
};

module.exports = { ExpressServer };